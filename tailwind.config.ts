import type { Config } from 'tailwindcss';

const config: Config = {
  // Tailwind v4 uses automatic content detection, no need for content array
  // Most configuration is now done in CSS using @theme directive
};

export default config;
